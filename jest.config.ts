import type {Config} from '@jest/types';

const config:Config.InitialOptions = {
   
        preset: "ts-jest",
        //codeCoverage: true,
        watchAll: true,
        verbose: true,
    
}

export default config;